<nav class="navbar navbar-expand-sm bg-dark shadow navbar-dark" aria-label="Third navbar example">
  <div class="container">
    <a class="navbar-brand" href="#">Kättbo VVO</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample03"
      aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse " id="navbarsExample03">
      <ul class="navbar-nav navbar-tabs me-auto mb-2 mb-sm-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ url_for('main.home') }}">Hem</a>
        </li>
        <li class="nav-item text-light">
          <a class="nav-link" href="{{ url_for('events.list_events') }}">Händelser</a>
        </li>
        <li class="nav-item">

        </li>
      </ul>
      {% if current_user.is_authenticated %}
        {% set user_roles = [] %}
        {% for role in current_user.roles %}
            {% if role.name not in user_roles %}
                {% do user_roles.append(role.name) %}
            {% endif %}
        {% endfor %}
      {% if 'admin' in user_roles or 'hunt-leader' in user_roles%}
      <div class="p-2 bd-highlight">
        <div class="btn-group" role="group">
          <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
            Admin
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{ url_for('admin.edit_hunt_teams')}}">Jaktlag</a></li>
            <li><a class="dropdown-item" href="#">Dropdown link</a></li>
          </ul>
        </div>

        <div class="btn-group">
          <a href="{{ url_for('events.create_event')}}" class="btn btn-outline-secondary" type="button">
            Ny händelse
          </a>
          <button type="button" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split"
            data-bs-toggle="dropdown" aria-expanded="false">
            <span class="visually-hidden">Toggle Dropdown</span>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="" disabled>SMS</a></li>
            <li><a class="dropdown-item" href="" disabled>Användare</a></li>
            <li><a class="dropdown-item" href="" disabled>Påminnelse</a></li>
          </ul>
        </div>
      </div>
      {% endif %}
      <a href="{{ url_for('users.update_profile') }}" class="btn btn-outline-light me-2">Ändra profil</a>
      <a href="{{ url_for('security.logout') }}" class="btn btn-danger">Logga ut</a>
      {% else %}
      <a href="{{ url_for('security.login') }}" class="btn btn-outline-dark me-2">Logga in</a>
      <a href="{{ url_for('security.register') }}" class="btn btn-warning">Registrera</a>
      {% endif %}
    </div>
  </div>
</nav>